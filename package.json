{
  "name": "todo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "cp .nojekyll dist/.nojekyll && gh-pages -d dist"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.11.0",
    "nuxt": "^3.16.2",
    "pinia-plugin-persistedstate": "^4.2.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
